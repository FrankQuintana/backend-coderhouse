<div>
    <h1>Todos los productos</h1> <a href="/realtimeproducts"><h3>RealTimeProducts</h3></a>
    {{#each productos}}
        <div>
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
            <p>{{this.category}}</p>
            <p>{{this.stock}}</p>
        </div>
    {{/each}}
</div>